import { match } from "./libs/match";

export { match };

export const {
    anyEnvironment,
    anyMacro,
    anyString,
    argument,
    blankArgument,
    comment,
    environment,
    group,
    macro,
    math,
    parbreak,
    string,
    whitespace,
} = match;
